# adblocker-tui

A Ratatui-based TUI for [`adblocker`](../README.md).

```sh
cargo build --release
sudo target/release/adblocker-tui                  # local Linux
target/release/adblocker-tui --ssh user@host       # remote (works on macOS)
```

Full docs: [../docs/TUI.md](../docs/TUI.md).
